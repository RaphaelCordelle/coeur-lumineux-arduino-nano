# ESP-IDF GPIO blink

Early ESP-IDF bring-up experiment that toggles an external LED on GPIO 23 once per second and logs its state. It is preserved for development traceability and is not part of the Arduino Nano LED-heart architecture.

The generated `build/` directory and local `sdkconfig` are intentionally excluded from version control.
